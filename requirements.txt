# Transcription plugin dependencies.
#
# Install torch + torchaudio SEPARATELY FIRST from the CUDA index (NOT the default PyPI CPU
# build) -- the /transcribe-setup wizard does this. For an NVIDIA GPU:
#   python -m pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
# Then install the rest:
#   python -m pip install -r requirements.txt

whisperx>=3.8.5
faster-whisper>=1.2.1
pyannote-audio>=4.0.4
yt-dlp>=2026.3.17
jsonschema>=4.21
rfc3339-validator>=0.1.4
imagehash>=4.3
Pillow>=10.0
numpy>=1.24
pandas>=2.0
