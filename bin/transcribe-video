#!/usr/bin/env bash
# CLI wrapper: transcribe video (URL or file). Add this bin/ dir to PATH to use from any terminal.
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python "$DIR/skills/transcribe-video/scripts/transcribe.py" "$@"
