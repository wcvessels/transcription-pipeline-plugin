{
  "name": "transcription",
  "description": "Local, token-free audio + video transcription with speaker diarization and change-detection screenshot curation (WhisperX + a token-free pyannote clone). Windows + NVIDIA GPU, CPU fallback.",
  "version": "1.2.0",
  "author": {
    "name": "Will Vessels"
  }
}
